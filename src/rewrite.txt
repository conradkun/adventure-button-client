<BrowserRouter>
  <div>
    <ul>
      <li><Link to="/">Home</Link></li>
      <li><Link to="/posts">Posts</Link></li>
      <li><Link to="/about">About</Link></li>
    </ul>

    <hr/>

    <Route path="/" exact component={Home} />
    <Route path="/posts" render={() => (
      <Posts
        value1={1}
        value2={2}
        data={this.state.data}
      />
    )} />
    <Route path="/about" component={About} />
  </div>
</BrowserRouter>

Complete rewrite using React router v4 (check the documentation for example)
Add route transition
Make a dynamic sidenav and header
(with a list of object dynamically populating the route)

Design:
Add huge drop shadow to card
Make the button like pill (check slide framework for example)
Make everything white, just our brand color on the top
https://github.com/Khan/aphrodite
https://webgradients.com/?ref=producthunt

